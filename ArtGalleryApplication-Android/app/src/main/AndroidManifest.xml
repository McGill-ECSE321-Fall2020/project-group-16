<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.mcgill.ecse321.artgalleryapplication">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArtGalleryApplicationAndroid">

        <!--Declaration of the SignUp activity as the first (default)-->
        <activity
            android:name=".SignUp"
            android:label="@string/app_name"
            android:theme="@style/Theme.ArtGalleryApplicationAndroid.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--Declaration of other activities-->
        <activity
            android:name=".Login"
            android:label="@string/app_name"
            android:theme="@style/Theme.ArtGalleryApplicationAndroid.NoActionBar">
        </activity>
        <activity
            android:name=".Home"
            android:label="@string/app_name"
            android:theme="@style/Theme.ArtGalleryApplicationAndroid.NoActionBar">
        </activity>
        <activity
            android:name=".Checkout"
            android:label="@string/app_name"
            android:theme="@style/Theme.ArtGalleryApplicationAndroid.NoActionBar">
        </activity>

    </application>

</manifest>